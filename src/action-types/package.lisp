;;;; src/action-types/package.lisp
;;;;
;;;; Placeholder file. Every action-type executor lives directly in
;;;; the :ldl.core package (see the other files in this directory); this
;;;; file exists only because ldl.asd lists a "package" component for the
;;;; action-types module, and has nothing of its own to add.

(in-package :ldl.core)
